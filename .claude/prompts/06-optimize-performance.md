# Optimize Performance

```
Помоги оптимизировать React-приложение (React 18, CRA).

Контекст: портфолио-сайт. Тяжёлые зависимости: framer-motion, swiper, i18next. Бандл анализирую через source-map-explorer (npm run analyze).

Предложи:
- Где применить React.lazy + Suspense (роуты через react-router v6)
- Что можно грузить лениво (Swiper, тяжёлые секции)
- Мемоизацию там, где есть реальные лишние ре-рендеры
- Оптимизацию изображений и i18n-ресурсов

Для каждого пункта: проблема → конкретное решение с кодом → ожидаемый эффект. Без преждевременной оптимизации.
```
