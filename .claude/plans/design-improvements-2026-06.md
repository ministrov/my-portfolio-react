# Дизайн-улучшения портфолио — июнь 2026

Анализ на основе скриншотов четырёх разрешений:
`390×844` (mobile), `768×1024` (tablet), `1366×768` (laptop), `1920×1080` (desktop).

Принцип: эволюционные правки без кардинальных переделок — каждый этап независим и безопасен.

---

## Этап 1 — Структурные пустоты (высокий приоритет): Согласен, одобренно

Самая заметная проблема на всех четырёх скриншотах: секции `.services` и `.advantages`
имеют `min-height: 78vh`, что создаёт огромные пустые зоны при прокрутке,
особенно на мобильном и планшете.

**Задачи:**

- [ ] `sections/services/style.css` — убрать `min-height: 78vh` из `.services`
- [ ] `sections/advantages/style.css` — убрать `min-height: 78vh` из `.advantages`
- [ ] Проверить, что визуальный ритм не разрушился на всех четырёх разрешениях

---

## Этап 2 — Заголовки секций : Важно: НЕ Согласен, делать не нужно

`.heading-sec__main::after` (линия-декор под заголовком) — сплошной синий, 48px.
Слишком мало для таких крупных заголовков (3.8rem), линия теряется.

**Задачи:**

- [ ] `components/heading/style.css` — сделать `::after` градиентным (`var(--gradient)`) вместо однородного синего
- [ ] Увеличить длину линии с `48px` до `64px`
- [ ] Проверить секцию about, где `::after` скрыт через `display: none` — не трогать

---

## Этап 3 — Карточки преимуществ (bento-сетка) Согласен, делай

`.advantages__item` — только рамка `3px solid #e9f0f5`, без тени.
На светлом фоне карточки почти сливаются. Нет hover-эффекта.

**Задачи:**

- [ ] `sections/advantages/style.css` — добавить `box-shadow: var(--box-shadow)` к `.advantages__item`
- [ ] Добавить hover: `box-shadow` усиливается + лёгкий `translateY(-4px)` (как у services)
- [ ] Цвет рамки заменить с `#e9f0f5` на `rgba(0, 88, 167, 0.1)` (в тон с about\_\_stat)

---

## Этап 4 — Hover-анимация сервисных карточек : Согласе, можно поглубже

`.services__item:hover { transform: translateY(-8px) }` — слишком резкий подъём.
На ноутбуке и десктопе смотрится дёргано.

**Задачи:**

- [ ] `components/servicesItem/style.css` — уменьшить `translateY` с `-8px` до `-5px`
- [ ] Добавить `box-shadow` на hover вместо (или вместе с) translate для ощущения подъёма

---

## Этап 5 — Project card: замена рамки на тень : Согласен, делай

`.project-card` использует `border: 1px solid var(--color-blue-700)` — жёсткая синяя рамка.
На ноутбуке/десктопе карточки выглядят слишком «клетчато».

**Задачи:**

- [ ] `components/projectCard/style.css` — заменить синий border на `border: 1px solid rgba(0, 88, 167, 0.2)`
- [ ] Добавить `box-shadow: 0 2px 16px rgba(0, 88, 167, 0.08)` по умолчанию
- [ ] На hover: `box-shadow: 0 8px 32px rgba(0, 88, 167, 0.18)` + `translateY(-3px)`
- [ ] Проверить `.project-card__image` — там тоже синий border, смягчить аналогично

---

## Этап 6 — Контактная форма: визуальное равенство карточек : Согласен, делай

`.contact__info` имеет `border-left: 4px solid var(--color-blue-700)` — акцентная полоска.
`.contact__form-wrap` — обычный border без акцента. Карточки визуально разного веса.

**Задачи:**

- [ ] `sections/contact/style.css` — добавить `border-left: 4px solid var(--color-blue-700)` к `.contact__form-wrap`
- [ ] Выровнять `padding` и `border-radius` обеих карточек (сейчас одинаковые, проверить на мобильном)

---

## Этап 7 — Testimonials: мягче затухание неактивных слайдов: Согласен, делай

`.testimonials__slide:not(.swiper-slide-active) { opacity: 0.4 }` — 40% это очень темно.
Соседние карточки почти не видны на планшете.

**Задачи:**

- [ ] `sections/testimonials/style.css` — поднять opacity с `0.4` до `0.55`

---

## Этап 8 — Цветовой токен `--color-gray-600`: Согласен, делай

Текущее значение `#f7f7f7c7` содержит альфа-канал (78% непрозрачность).
Используется как фон «карточных» секций (about, advantages, faq, contact).
На некоторых экранах/браузерах это создаёт неожиданный эффект просвечивания.

**Задачи:**

- [ ] `styles/main.css` — заменить `--color-gray-600: #f7f7f7c7` на `--color-gray-600: #f5f8ff`
      (лёгкий холодный оттенок — сочетается с синей палитрой, убирает прозрачность)
- [ ] Проверить все секции, использующие `var(--color-gray-600)` как фон

---

## Этап 9 — Типографика: `wrapper` отступ на десктопе: Согласен, делай

`.wrapper { margin-top: 120px }` — очень большой разрыв между шапкой и первой секцией
на десктопе (1920px). Hero занимает 78vh, потом ещё 120px до About.

**Задачи:**

- [ ] `styles/main.css` — уменьшить `margin-top: 120px` до `80px`
- [ ] Проверить, что Header не перекрывает контент на всех разрешениях

---

## Этап 10 — Мелкие мобильные правки (390px): Согласен, делай

Наблюдения по мобильному скриншоту:

**Задачи:**

- [ ] Hero-кнопка на `<= 475px` уже full-width — добавить стрелку-иконку `→` если её нет
- [ ] About section: соцсети-иконки (`components/socials`) — проверить отступы на 390px
- [ ] FAQ accordion: проверить читаемость вопросов при `font-size` меньше 16px

---

## Очерёдность выполнения

| Этап | Файлы                                    | Сложность | Эффект  |
| ---- | ---------------------------------------- | --------- | ------- |
| 1    | services/style.css, advantages/style.css | низкая    | высокий |
| 2    | components/heading/style.css             | низкая    | средний |
| 8    | styles/main.css                          | низкая    | средний |
| 3    | sections/advantages/style.css            | низкая    | средний |
| 4    | components/servicesItem/style.css        | низкая    | низкий  |
| 5    | components/projectCard/style.css         | средняя   | высокий |
| 6    | sections/contact/style.css               | низкая    | низкий  |
| 7    | sections/testimonials/style.css          | низкая    | низкий  |
| 9    | styles/main.css                          | низкая    | средний |
| 10   | несколько компонентов                    | средняя   | средний |
